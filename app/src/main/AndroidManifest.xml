<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fragmentsrecycler">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logorent"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fragmentsrecycler">
       <activity
            android:name=".Signin"
           android:exported="true">
            <intent-filter>
              <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" >

        </activity>
        <activity android:name=".OwnerActivity"></activity>
        <activity android:name=".RenterActivity"></activity>
        <activity android:name=".OwnerPoint"></activity>
        <activity android:name=".Renterview"></activity>
        <activity android:name=".ownerview"></activity>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>